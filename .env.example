# 阿里云 OSS 相册同步配置（复制本文件为 .env 并填写）
# 注意：.env 已被 .gitignore 忽略，请勿提交真实凭据

# OSS 区域，如 oss-cn-hangzhou / oss-cn-beijing / oss-cn-shenzhen
OSS_REGION=oss-cn-hangzhou

# OSS Bucket 名称
OSS_BUCKET=my-bucket

# RAM 子账号 AccessKey（建议使用最小权限 RAM 用户，仅需 ListObjects/GetObject 权限）
OSS_ACCESS_KEY_ID=your-access-key-id
OSS_ACCESS_KEY_SECRET=your-access-key-secret

# 可选：自定义 CDN 域名（如 images.example.com，需已绑定到该 bucket 并开启公开读）
# 不填则使用 https://<bucket>.<region>.aliyuncs.com 默认域名
OSS_DOMAIN=

# 发现模式（pnpm sync-oss --discover）的相册根前缀。
# 扫描该前缀下的每个直接子目录，各自成为一个相册。
OSS_GALLERY_ROOT=picture/favorites

# 手动模式（pnpm sync-oss --album <id> / --all）的文件前缀，实际路径为 <前缀>/<相册id>/
OSS_ALBUM_PREFIX=albums

# 微信读书 API Key（用于构建时获取书架与阅读数据，详见 docs/adr/0004）
# 在微信读书 App -> 我 -> 关注微信公众号「微信读书」或相关入口获取，格式 wrk-xxxxxxxx
WEREAD_API_KEY=
