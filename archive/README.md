# archive

已从站点移除、但保留备查的内容。放在这里的东西**不会**被 Astro 构建或部署，
因为它不在 `src/content/posts/` 之下（内容集合的扫描范围）。

## posts/

Firefly 主题自带的「文章示例」（`category: 文章示例`），用于演示各种 Markdown/MDX 语法
（代码块、Mermaid、PlantUML、KaTeX 公式、视频嵌入、加密文章等）。这些不是本站的正式内容，
故从博客展示中移除，保留于此以便日后查阅语法写法。

- 共 11 篇，含 `images/` 下它们引用的本地图片（已一并复制，可直接用 Markdown 阅读器打开）
- 图片是副本，站点使用的那份仍在 `src/content/posts/images/`

若想恢复某篇，把它移回 `src/content/posts/` 即可；图片引用路径 `./images/xxx`
在两边都成立。
