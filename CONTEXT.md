# CONTEXT

本项目的领域词汇表。只收录术语定义，不包含实现细节或技术方案。

## 相册（Album）

站点相册页中一张可点击的卡片，对应一个 URL（`/gallery/<album id>/`）和一组照片。
每张卡片有名称、可选描述/日期/地点/标签。

## 照片（Photo）

相册中的一张图片，有一个可访问的图片 URL。照片可以来自站内本地文件，也可以来自
外部对象存储（阿里云 OSS）。

## 相册根（Album Root）

对象存储中一个约定的目录前缀，是**发现相册的起点**。相册根本身不是相册，而是相册的容器：
它下面的每个子目录各是一个相册。相册根之外的内容不属于相册体系。

## 自动发现相册（Discovered Album）

由同步流程扫描相册根下面直接子目录而自动得出的相册。它的身份（album id）就是子目录名。
它的元数据来自该目录内的元数据文件；元数据文件缺失时，目录名同时充当相册名称。

## 手写相册（Manual Album）

在站点源码里显式声明的相册，元数据由人工逐字段填写。与自动发现相册同 id 时，手写相册优先。

## 相册元数据（Album Metadata）

描述一个相册的字段集合：名称（必有）、描述、日期、地点、标签、封面、访问密码、密码提示。
对自动发现相册而言，这些字段存放在相册目录内的元数据文件里。

## 相册封面（Album Cover）

相册卡片上展示的代表图。来源优先级：元数据显式指定 > 目录内特定命名的文件 > 目录内第一张照片。

## 设置项（Setting）

访客或站主可调整的一项站点显示偏好（如主题色相、壁纸模式、横幅开关）。每个设置项有一个
稳定的身份和一个存储键；访客的取值持久保存在浏览器本地，未设置时回落到站点配置的默认值。

## 沉浸首页（Immersive Home）

首页的一种形态：整屏封面背景、居中的打字机标题，向下滚动才进入文章列表。它是一个可开关的
设置项；关闭时首页回落到常规版式。

## 微信读书模块（WeRead）

站点"我的"菜单下的一个展示页，呈现站主在微信读书平台的阅读情况。数据来自微信读书官方
接口，在构建时获取；它是纯展示页，不提供在线阅读。

## 在读书籍（Reading Book）

书架中尚未读完、正在阅读的书籍，在微信读书模块的"在读"栏展示，附阅读进度。

## 已读书籍（Finished Book）

书架中已读完的书籍（微信读书的"读完"标记），在微信读书模块的"读完"栏展示。

## 阅读统计（Reading Stats）

一个统计周期（如本月）内的阅读汇总数字：阅读时长、有效阅读天数、读完本数等，
在微信读书模块顶部以摘要卡展示。

## 私密阅读（Private Reading）

微信读书里一项书籍可见性标记（不向微信好友展示）。它只描述微信读书站内的社交可见性，
与博客是否展示该书是两个独立的决定，由博客侧的展示策略另行约束。
